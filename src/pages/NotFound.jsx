import React from 'react';

import NotFoundComponent from '../components/app//NotFound/NotFound';

const NotFound = (props) => {
  return (
    <NotFoundComponent {...props}/>
  );
};

export default NotFound;
